# Bootstrap Spec Usage

The consolidated project guidance is in `.opencode/instructions/socrates-spec.md`.

Usage rules:

- Load only sections needed for the active task.
- If guidance conflicts, priority is:
  1. `AGENTS.md`
  2. `.opencode/rules/*.md`
  3. `.opencode/instructions/socrates-spec.md`
- Never assume research archive pages are implementation truth.
